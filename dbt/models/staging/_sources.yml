version: 2

sources:
  - name: raw
    database: economic-pulse
    schema: raw
    tables:
      - name: raw_fred_observations
        config:
          loaded_at_field: loaded_at
          freshness:
            warn_after: { count: 40, period: day }
            error_after: { count: 100, period: day }
